using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.T))
        //{
        //    Enemy enemy = Enemy.Create(Tools.GetMouseWorldPosition() + Tools.GetRandomeDirection());
        //    enemy.GetComponent<HealthSystem>().SetMaxHP(100);
        //}
    }
}
